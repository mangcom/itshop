# 🛒 IT Shop Management System

ระบบบริหารจัดการและหน้าร้านขายอุปกรณ์คอมพิวเตอร์ พัฒนาด้วย PHP (PDO), MySQL, และเทคโนโลยีเว็บสมัยใหม่ (Bootstrap 5, DataTables, SweetAlert2) รองรับระบบจัดการหลังบ้าน (Admin Panel) และตะกร้าสินค้าฝั่งหน้าบ้าน (Client-side Cart)

## 🌟 คุณสมบัติเด่น (Features)

- **Admin Dashboard**: สรุปภาพรวมสินค้าและหมวดหมู่
- **Product Management**: ระบบ CRUD สินค้า แยก "รุ่น" และ "Model" พร้อมระบบ Auto-complete ยี่ห้อ
- **Category System**: จัดการหมวดหมู่สินค้าได้อิสระ
- **AJAX Driven**: การทำงานเป็นแบบ Single-page experience ไม่ต้อง Refresh หน้าจอ
- **Client Cart**: ระบบตะกร้าสินค้าจำลองสำหรับลูกค้า เก็บข้อมูลผ่าน Cookie นาน 12 ชั่วโมง
- **File Management**: รองรับการอัปโหลดรูปภาพสินค้าและไฟล์ PDF Specsheet

---

## 🛠 การติดตั้ง (Installation)

### 1. เตรียมฐานข้อมูล

นำไฟล์ SQL (ถ้ามี) ไปรันในระบบจัดการฐานข้อมูลของคุณ หรือสร้างตารางตามโครงสร้างที่กำหนด

### 2. การตั้งค่า Environment (.env)

สร้างไฟล์ชื่อ `.env` ไว้ที่ Root Directory ของโปรเจกต์ และกำหนดค่าเชื่อมต่อฐานข้อมูลดังนี้:

```env
DB_HOST="host_address_or_name"
DB_NAME="itshop"
DB_USER="database_user"
DB_PASS="database_password"
```

### 3. ตั้งค่าสิทธิ์โฟลเดอร์ (Permissions)

เพื่อให้ระบบสามารถอัปโหลดรูปภาพและไฟล์ PDF ได้ ต้องทำการตั้งค่าสิทธิ์ให้โฟลเดอร์ uploads:

```
chmod -R 777 uploads/
```

(หรือตรวจสอบให้แน่ใจว่า Web Server มีสิทธิ์เขียนไฟล์ลงในโฟลเดอร์ images และ docs ย่อยได้)

### 4. โครงสร้าง Project

```
itshop/
├── admin/                # ระบบจัดการหลังบ้าน (Admin Panel)
│   ├── api/              # Backend API (JSON Response)
│   ├── assets/           # CSS และ JS สำหรับ Admin
│   ├── includes/         # ไฟล์ส่วนประกอบ (Navbar, Sidebar, Header, Footer)
│   └── *.php             # หน้าจอการทำงานหลักของ Admin
├── assets/               # CSS และ JS สำหรับหน้าบ้าน (Customer)
├── uploads/              # โฟลเดอร์เก็บไฟล์อัปโหลด
│   ├── images/           # รูปภาพสินค้า
│   └── docs/             # ไฟล์ PDF Datasheet
├── .env                  # ไฟล์ตั้งค่าฐานข้อมูล (ไม่ควรนำขึ้น GitHub)
├── db.php                # ไฟล์เชื่อมต่อฐานข้อมูลหลัก
├── index.php             # หน้าหลักแสดงสินค้าสำหรับลูกค้า
└── cart.php              # หน้าตะกร้าสินค้าและสรุปรายการ
```

### 5 . 🔐 ข้อมูลผู้ดูแลระบบเริ่มต้น (Default Admin)

Username: admin

Password: (กรุณาใช้ไฟล์ emergency_reset.php หากต้องการตั้งรหัสใหม่ และควรลบไฟล์นี้ทิ้งหลังจากแก้ไขแล้ว)

### 6. 📝 รายละเอียดเพิ่มเติม

Tech Stack: PHP 7.4+, MariaDB/MySQL, jQuery, Bootstrap 5

Security: มีระบบตรวจสอบการ Login และใช้ Password Hashing ในการเก็บรหัสผ่าน
